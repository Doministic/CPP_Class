
#include <iostream>
#include <list>
/*
int main()
{
	int numRolls = 0;
	int numDice = 0;

	int diceSidesVal[6] = { 6, 5, 4, 3, 2, 1 };
	std::list<int> diceRolledVal = {};
	std::list<int> second = {};

	for (int i = 1; i <= sizeof(diceSidesVal) / sizeof(int); i++)
	{
		diceSidesVal[i - 1] = diceSidesVal[i - 1] * 3;
		diceRolledVal.push_back(diceSidesVal[i - 1]); // {18, 15
		second.push_front(diceSidesVal[i - 1]); // {18, 15
	}

	int i = 0;
	for (int n : diceRolledVal)
	{
		std::cout << "Index :" << i << " Push Back: " << n << "\n";
	}
	std::cout << "\n\n\n\n";
	for (int n : second)
	{
		std::cout << "Index :" << i << " Push Front: " << n << "\n";

	}

	std::cout << "Done!";

	return 0;
}
*/